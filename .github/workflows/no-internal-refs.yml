name: Public-surface gate

# Founder directive 2026-07-27: no internal project bookkeeping on any surface a consumer
# reads. Item identifiers (`CLI-6`), phase and wave language, ADR numbers, roadmap
# citations, meta-repo paths and process commentary belong in the changeset, CHANGELOG.md,
# the commit, the PR and the roadmap, never in README.md, docs-content/, the npm
# description, a JSDoc block a consumer's editor renders, or a diagnostic a consumer reads
# in their terminal. The rule is written out in the meta-repo's
# `documentation/conventions.md` ("No internal project bookkeeping on a public surface");
# `scripts/check-no-internal-refs.sh` is what enforces it.
#
# WHY A GATE AND NOT A NOTE, and in this repo the answer is not theoretical. `@cosyte/cli`
# shipped an internal work item inside `CLI_NOT_IMPLEMENTED`'s message and an ADR number
# inside `CLI_PARSER_UNAVAILABLE`'s, both printed to a user's terminal and both compiled
# into `dist/index.d.ts`. They were cut by hand. Nothing stopped them being written, and
# nothing would have stopped the next one. The founder, on the ecosystem-wide sweep: "it
# needs to not just be a memory note, but something that is addressed in the workflow
# accordingly. This needs to not happen again."
#
# WHY A SEPARATE WORKFLOW, and not a job in ci.yml:
#
#   The reusable pipeline is not an option. This repo's `ci` job calls
#   cosyte/.github/.github/workflows/ci.yml@main, which runs a fixed ladder and no arbitrary
#   repo script. Adding this there would also flip every other caller red on its next run,
#   and remediating each repo is its own deliberate piece of work. A shared pipeline is the
#   wrong place to land a rule most of its callers cannot yet satisfy.
#
#   Keeping it out of ci.yml also keeps it cheap: this job is a checkout and a handful of
#   greps over 12 markdown/JSON files and 27 source files, while ci.yml drives the full
#   Node 22 + 24 matrix plus the release dry run.
#
# WHAT THIS DOES NOT CHECK, deliberately: the PR title, the PR body, and commit messages.
# This rule says identifiers BELONG in the commit, the PR and the changeset, so a PR-text
# half would red on correct work. That is also why there is no `edited` trigger: nothing
# here reads PR text, so retitling a PR cannot change the result.
#
# The other half of this rule, the one that keeps identifiers out of a published RELEASE
# BODY, lives in the shared pipeline: `cosyte/.github` `scripts/release-notes.mjs assert`
# re-reads the finished body bytes and refuses to publish a violating one. This job covers
# the repo's own files; that one covers what gets published to the releases page.
#
# Run it locally with `pnpm check:no-internal-refs`, which is also on the meta-repo's
# `scripts/verify.sh cli` ladder.

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  # The job id is the check-run context name a branch ruleset requires. Renaming it
  # silently detaches the required check, so the ruleset stops protecting anything while
  # still looking configured. Rename both together or neither. There is no `paths:` filter
  # here on purpose: a required context that a path filter can skip leaves a PR pending
  # forever.
  no-internal-refs:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0

      # The script self-tests before it reports: it proves each rule still matches its own
      # positive sample AND still lets through the reference material it was most likely to
      # destroy. That negative half matters more here than in any sibling, because this is
      # the package that wraps all eight formats: `HL7-V2`, `FHIR-R4`, `DICOM-SR`,
      # `NCPDP-SCRIPT`, `X12-837P`, `CCDA-R2.1`, `MSH-2`, `NM1-03`, `439-E4`, `ICD-10-CM`
      # and a Phase III trial all have to survive it. It refuses to print OK from a scan
      # that did not read all of its input. No `|| true`, no `continue-on-error`: a check
      # that cannot fail is documentation.
      - name: Check the public surface
        run: bash scripts/check-no-internal-refs.sh
